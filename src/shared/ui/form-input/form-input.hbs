<div class="form__group {{#if isInvalid}}invalid{{/if}}">
    {{{input}}}
    <label 
        for={{name}} 
        class="form__label"
    >{{label}}</label>
    {{{feedback}}}
    {{{feedbackError}}}
</div>