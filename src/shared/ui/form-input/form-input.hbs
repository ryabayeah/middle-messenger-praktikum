<div class="form__group {{#if isInvalid}}invalid{{/if}}">
    <label 
        for={{name}} 
        class="form__label"
    >{{label}}</label>
    {{{input}}}
    {{{feedback}}}
    {{{feedbackError}}}
</div>