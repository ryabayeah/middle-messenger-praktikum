<input 
autocomplete="off"
type={{type}}
class="form__input {{#if isInvalid}} 'invalid' {{/if}}"
name={{name}}
id={{id}}
{{#if value}} value="{{value}}" {{/if}}
{{#if isDisabled}} disabled {{/if}}
{{#if isRequired}} required {{/if}}
/>